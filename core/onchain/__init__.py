# Phase 5.5: On-Chain Integration
# Glassnode (netflow), DefiLlama (stablecoins), Binance (cumulative delta)
# LLM → Post-Processor → on_chain_bias → composite
# Spec: docs/07-on-chain-integration.md
